import React, {useEffect, useRef, useState} from 'react';

export type LottieProps = {}

export default function Lottie({}: LottieProps) {
    const ref = useRef()
    const [state, setState] = useState(null)
    useEffect(() => {

    })

    return (<div className="Lottie"/>)
}
